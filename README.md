# go-api

Run on Dev environment with port 8080

```bash
    cp env to .env
    air
```

Deploy with Cloud run set port 8080

```bash
    cp env to .env.production
    build-image-gcloud
    push-image-gcloud
```
